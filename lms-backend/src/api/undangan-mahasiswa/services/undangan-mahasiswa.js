'use strict';

/**
 * undangan-mahasiswa service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::undangan-mahasiswa.undangan-mahasiswa');
