'use strict';

/**
 * undangan-mahasiswa router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::undangan-mahasiswa.undangan-mahasiswa');
